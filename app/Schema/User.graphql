type Query {
  allUsers(paginate: Paginate): [User],
}

type Mutation {
  addUser(user: UserInput!): User
}

type User {
  email: String!,
  username: String!
  created_at: String!,
  updated_at: String!
}